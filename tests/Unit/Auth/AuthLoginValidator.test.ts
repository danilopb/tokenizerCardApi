// import AuthLoginValidator from "../../../src/Auth/Infrastructure/Validators/AuthLoginValidator";

describe('AuthLoginValidator_class', () => {

    it("test_auth_login_validator_validate", () => {
        // const validator = new AuthLoginValidator();
        // const parameters = { username: "validUser", password: "validPassword" };

        // expect(() => validator.validate(parameters)).not.toThrow();
    });

});
